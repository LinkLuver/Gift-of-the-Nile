country_decisions = {
	
	# Form Phrygia
	form_phrygia = {
		
		potential = {
			num_of_cities >= 1
			tag = PHR
			NOT = {
				is_endgame_tag_trigger = yes
				is_tier_2_formable_trigger = yes
			}
		}
		
		highlight = {
			scope:province = {
				OR = {
					is_in_area = galatia_trocmi_area
					is_in_area = galatia_area
					is_in_area = phrygia_maior_area
					is_in_area = bithynia_superior_area
					is_in_area = synnada_area
					is_in_area = phrygia_epiktetos
				}
			}
		}
		
		allow = {
			can_form_nation_trigger = yes
			owns_or_subject_owns_area = galatia_trocmi_area
			owns_or_subject_owns_area = galatia_area
			owns_or_subject_owns_area = phrygia_maior_area
			owns_or_subject_owns_area = bithynia_superior_area
			owns_or_subject_owns_area = phrygia_epiktetos
			owns_or_subject_owns_area = synnada_area
		}
		
		effect = {
			custom_tooltip = "tier_2_formable_tooltip"
			tribal_formable_government_change_effect = yes
			change_country_name = "PHRYGIA_NAME"
			change_government = aristocratic_monarchy
			hidden_effect = {
				change_country_adjective = "PHRYGIA_ADJECTIVE"
				change_country_color = "phrygia_red"
				change_country_tag = PRG
				change_country_flag = "PHRYGIA_FLAG"
			}
			capital_scope = {
				capital_formable_medium_effect = yes
				formable_capital_modifier_normal_effect = yes
			}
			custom_tooltip = phrygia_claims_tooltip
			hidden_effect = {
				region:galatia_region = { formable_region_province_effect = yes }
				region:asia_region = { formable_region_province_effect = yes }
				region:lycia_region = { formable_region_province_effect = yes }
				region:cilicia_region = { formable_region_province_effect = yes }
			}
			add_4_free_province_investments = yes
		}
		
		ai_will_do = {
			base = 1
		}
	}
	
}